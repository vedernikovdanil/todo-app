export enum TodoPriorityEnum {
  low = "low",
  medium = "medium",
  high = "high",
}

export enum TodoStatusEnum {
  toBeDone = "toBeDone",
  inProgress = "inProgress",
  done = "done",
  canceled = "canceled",
}
